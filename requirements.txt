# Dependencias de la app de Control de Calidad.
# Solo se usan para generar reportes/exportaciones; el runtime web usa stdlib.
# Instalar (una sola vez):  py -m pip install -r requirements.txt
openpyxl>=3.1
reportlab>=4.0
